interface profileConfigInterface {
    modalName: string,
    buttonText: string,
    pageName: string
}

export const profileConfig: profileConfigInterface = {
    modalName: "Get info modal",
    buttonText: "Submit",
    pageName: "Profile"
};